import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
  creditsPage: {
    position: 'relative',
    width: '100%',
    minHeight: '90vh',
    background: '#fff',
    paddingTop: '15px',
  },
}));
