import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
  whiteSection: {
    display: 'flex',
    background: '#fff',
  },
  leftColumn: {
    maxWidth: '900px',
    position: 'relative',
  },
  witeSectionWrapper: {
    display: 'flex',
  },
}));
